# 产品矩阵功能设计

## 功能模块架构

```
ProductMatrix 产品矩阵
├── Config Management (配置管理)
│   ├── VendorConfig (厂商管理)
│   ├── DimensionConfig (维度定义)
│   └── MatrixSettings (矩阵设置)
├── Product Management (产品管理)
│   ├── ProductList (产品列表)
│   ├── ProductForm (产品编辑表单)
│   ├── ProductDetail (产品详情)
│   └── ProductImport (批量导入)
├── Visualization (可视化)
│   ├── MatrixChart (矩阵图表)
│   ├── VendorFilter (厂商筛选器)
│   └── AxisSelector (轴选择器)
├── Analysis (分析模块)
│   ├── VendorDistribution (厂商分布图)
│   ├── PriceHeatmap (价格热力图)
│   └── CoverageAnalysis (覆盖率分析)
└── I/O (数据导入导出)
    ├── ExcelExporter (Excel 导出)
    ├── ExcelImporter (Excel 导入)
    └── TemplateGenerator (模板生成)
```

---

## 一、配置管理模块

### 1.1 厂商管理（VendorConfig）

**功能**：管理竞品厂商列表

**UI 组件**：
- Ant Design Table（可编辑表格）
- ColorPicker（复用 VendorManager 的 PowerPoint 调色板）
- 拖拽排序（@dnd-kit）

**表格列**：
| 列名 | 类型 | 说明 |
|------|------|------|
| 排序 | 拖拽手柄 | 拖拽调整顺序 |
| 厂商名称 | 可编辑文本 | 直接在单元格编辑 |
| 颜色 | 颜色选择器 | 点击打开调色板 |
| 可见性 | Switch | 显示/隐藏厂商产品 |
| 操作 | 按钮组 | 编辑、删除 |

**操作**：
- [+ 添加厂商]：新增一行
- [删除]：删除选中厂商（需确认，检查是否有关联产品）
- [导入]：批量导入厂商列表（Excel/JSON）

---

### 1.2 维度定义（DimensionConfig）

**功能**：定义产品维度及其取值范围

**UI 布局**：
```
┌────────────────────────────────────────┐
│ [+ 添加维度] [导入] [导出]              │
├────────────────────────────────────────┤
│ ► 维度 1: WiFi 协议 (离散)    [编辑] [删除] │
│   ├─ 选项: WiFi 5, WiFi 6, WiFi 6E, WiFi 7  │
│                                        │
│ ► 维度 2: 速率级别 (离散)    [编辑] [删除] │
│   ├─ 选项: AX1800, AX3000, AX3600, AX5400   │
│                                        │
│ ► 维度 3: 价格区间 (连续)    [编辑] [删除] │
│   ├─ 范围: 0 - 5000 元                │
│   └─ 分段: 5                          │
└────────────────────────────────────────┘
```

**添加/编辑维度弹窗**：
```
┌─────────────────────────────────────┐
│ 添加维度                             │
├─────────────────────────────────────┤
│ 维度名称: [___________________]      │
│                                     │
│ 维度类型: ( ) 离散型  (●) 连续型   │
│                                     │
│ ┌─ 离散型选项 ─────────────────┐   │
│ │ [+ 添加选项]                 │   │
│ │                               │   │
│ │ 选项列表:                     │   │
│ │   1. WiFi 5     [↑] [↓] [×]  │   │
│ │   2. WiFi 6     [↑] [↓] [×]  │   │
│ │   3. WiFi 6E    [↑] [↓] [×]  │   │
│ └───────────────────────────────┘   │
│                                     │
│ ┌─ 连续型设置 ─────────────────┐   │
│ │ 最小值: [0____]              │   │
│ │ 最大值: [5000_]              │   │
│ │ 单位:   [元____]             │   │
│ │ 建议分段: [5__]              │   │
│ └───────────────────────────────┘   │
│                                     │
│          [取消]  [确定]             │
└─────────────────────────────────────┘
```

**操作**：
- [+ 添加维度]：打开弹窗，选择类型并配置
- [编辑]：修改维度配置
- [删除]：删除维度（需确认，检查是否有产品使用）
- [拖拽排序]：调整维度显示顺序

**数据验证**：
- 维度名称不能为空，且不能重复
- 离散型至少需要 2 个选项
- 连续型最小值 < 最大值

---

### 1.3 矩阵设置（MatrixSettings）

**功能**：配置矩阵显示选项

**UI 表单**：
```
┌─────────────────────────────────────┐
│ 矩阵配置                             │
├─────────────────────────────────────┤
│ X 轴维度: [WiFi 协议       ▼]       │
│ Y 轴维度: [速率级别         ▼]       │
│                                     │
│ 显示选项:                           │
│   [√] 使用厂商颜色标识产品          │
│   [ ] 显示产品图片 (需上传图片)     │
│   [√] 显示价格标签                  │
│   [√] 显示空单元格                  │
│                                     │
│ 单元格布局:                         │
│   ( ) 堆叠 (垂直排列)               │
│   ( ) 网格 (横向+纵向排列)          │
│   (●) 重叠 (中心偏移，节省空间)     │
│                                     │
│ 单格最多显示: [10__] 个产品         │
│   (超过则折叠显示 "+N")             │
│                                     │
│ 图例:                               │
│   [√] 显示图例                      │
│   位置: [顶部 ▼]                    │
│                                     │
│          [重置]  [应用]             │
└─────────────────────────────────────┘
```

**实时预览**：配置改变时，矩阵图表立即更新

---

## 二、产品管理模块

### 2.1 产品列表（ProductList）

**功能**：展示和管理所有产品

**UI 组件**：Ant Design Table

**表格列**：
| 列名 | 宽度 | 说明 |
|------|------|------|
| 产品名称 | 200px | 品牌 + 型号 |
| 厂商 | 120px | 所属厂商（带颜色标识） |
| 价格 | 100px | 价格 + 单位 |
| [维度1] | 120px | 动态列（根据维度定义生成） |
| [维度2] | 120px | 动态列 |
| ... | 120px | ... |
| 操作 | 150px | 编辑、删除、复制 |

**工具栏**：
```
┌────────────────────────────────────────────────────┐
│ [+ 添加产品] [批量导入] [导出选中] [模板下载]      │
│                                                    │
│ 搜索: [_________] | 厂商: [全部 ▼] | 排序: [名称 ▼] │
└────────────────────────────────────────────────────┘
```

**功能**：
- **搜索**：按产品名称/型号模糊搜索
- **筛选**：按厂商、价格区间筛选
- **排序**：按名称、价格、发布日期排序
- **批量操作**：选中多个产品，批量删除/导出
- **分页**：每页 20 条（可配置）

**空状态**：
```
┌────────────────────────────────────┐
│         🔍                          │
│     暂无产品数据                    │
│                                    │
│  点击 [+ 添加产品] 开始录入        │
│  或使用 [批量导入] 快速导入        │
└────────────────────────────────────┘
```

---

### 2.2 产品编辑表单（ProductForm）

**功能**：添加或编辑产品信息

**UI 形式**：Modal 弹窗（宽度 600px）

**表单布局**：
```
┌─────────────────────────────────────┐
│ 添加产品                             │
├─────────────────────────────────────┤
│ ┌─ 基本信息 ───────────────────┐   │
│ │ * 产品名称: [_________________] │   │
│ │ * 所属厂商: [TP-Link    ▼]    │   │
│ │   品牌名称: [TP-Link_________] │   │
│ │   型号:     [Archer AX55____] │   │
│ │   价格:     [399_] 元         │   │
│ │   描述:     [_______________] │   │
│ │             [_______________] │   │
│ │   产品链接: [https://______] │   │
│ └───────────────────────────────┘   │
│                                     │
│ ┌─ 维度数据 ───────────────────┐   │
│ │ * WiFi 协议: [WiFi 6    ▼]   │   │
│ │ * 速率级别: [AX3000     ▼]   │   │
│ │   价格区间: [399_______] 元  │   │
│ │   ... (根据维度定义动态生成)  │   │
│ └───────────────────────────────┘   │
│                                     │
│ ┌─ 产品图片 (可选) ───────────┐   │
│ │ [上传图片] 或 [输入图片URL] │   │
│ │                               │   │
│ │  ┌───────────┐               │   │
│ │  │ 预览图片   │               │   │
│ │  └───────────┘               │   │
│ └───────────────────────────────┘   │
│                                     │
│          [取消]  [保存]             │
└─────────────────────────────────────┘
```

**字段说明**：
- **产品名称**：必填，建议格式 "品牌 型号"
- **所属厂商**：必填，下拉选择（从厂商列表）
- **品牌名称**：可选，自动填充厂商名称（可修改）
- **型号**：可选，但建议填写
- **价格**：可选，数值输入
- **维度数据**：必填（根据维度是否标记为必填）
  - 离散维度：下拉选择
  - 连续维度：数值输入（带单位显示）

**数据验证**：
- 产品名称不能为空
- 必填维度必须有值
- 价格必须为非负数
- 连续维度值必须在范围内

**自动填充**：
- 选择厂商后，自动填充品牌名称和厂商颜色

---

### 2.3 产品详情（ProductDetail）

**功能**：查看产品完整信息

**UI 形式**：Drawer 抽屉（宽度 480px）

**布局**：
```
┌────────────────────────────────────┐
│ TP-Link Archer AX55         [× 关闭] │
├────────────────────────────────────┤
│  ┌──────────────────────────────┐  │
│  │     产品图片（居中显示）      │  │
│  │      (如果没有则显示默认)     │  │
│  └──────────────────────────────┘  │
│                                    │
│  基本信息                          │
│  ─────────────────────────────────  │
│  厂商:     ● TP-Link (带颜色标识) │
│  品牌:     TP-Link                 │
│  型号:     Archer AX55             │
│  价格:     ¥399                    │
│  发布日期: 2023-05-15              │
│                                    │
│  描述                              │
│  ─────────────────────────────────  │
│  双频 WiFi 6 路由器，支持 3000Mbps │
│  组合速率，适合中小户型使用...     │
│                                    │
│  维度数据                          │
│  ─────────────────────────────────  │
│  WiFi 协议:   WiFi 6               │
│  速率级别:   AX3000                │
│  价格区间:   399 元                │
│  ...                               │
│                                    │
│  ┌────────────────────────────┐   │
│  │  [编辑产品]  [删除产品]     │   │
│  └────────────────────────────┘   │
│                                    │
│  相关链接                          │
│  ─────────────────────────────────  │
│  [🔗 官网链接]                     │
└────────────────────────────────────┘
```

---

### 2.4 批量导入（ProductImport）

**功能**：从 Excel 批量导入产品

**流程**：
1. [下载模板] → 获取 Excel 模板文件
2. [选择文件] → 上传填好的 Excel
3. [数据预览] → 显示解析结果和错误
4. [确认导入] → 导入到系统

**Excel 模板格式**：

**工作表 1: 产品列表**
| 产品名称* | 厂商* | 品牌 | 型号 | 价格 | 描述 | WiFi 协议* | 速率级别* | ... |
|-----------|-------|------|------|------|------|------------|-----------|-----|
| TP-Link Archer AX55 | TP-Link | TP-Link | Archer AX55 | 399 | 双频路由器 | WiFi 6 | AX3000 | ... |

**工作表 2: 维度选项参考**（只读，供填表参考）
| WiFi 协议 | 速率级别 | ... |
|-----------|----------|-----|
| WiFi 5    | AX1800   | ... |
| WiFi 6    | AX3000   | ... |
| WiFi 6E   | AX3600   | ... |
| WiFi 7    | AX5400   | ... |

**数据验证**：
- 必填字段检查
- 厂商名称匹配（必须存在于系统中，否则提示新增）
- 维度值合法性（离散值必须在选项中，连续值在范围内）
- 重复产品检测（按名称或型号）

**导入预览界面**：
```
┌────────────────────────────────────────┐
│ 批量导入产品                            │
├────────────────────────────────────────┤
│ 文件: products.xlsx                    │
│ 解析结果: 成功 45 条，失败 5 条        │
│                                        │
│ Tab: [ 成功 (45) ] [ 失败 (5) ]       │
│                                        │
│ ┌──────────────────────────────────┐  │
│ │ 成功列表 (将导入的数据)           │  │
│ │ ─────────────────────────────────  │  │
│ │ 1. TP-Link Archer AX55            │  │
│ │ 2. 华为 AX3 Pro                   │  │
│ │ ...                               │  │
│ └──────────────────────────────────┘  │
│                                        │
│ ┌──────────────────────────────────┐  │
│ │ 失败列表 (需修正)                 │  │
│ │ ─────────────────────────────────  │  │
│ │ 第 3 行: 厂商 "Cisco" 不存在      │  │
│ │ 第 7 行: WiFi 协议值 "WiFi 8" 无效│  │
│ │ 第 12 行: 价格必须为数字          │  │
│ │ ...                               │  │
│ └──────────────────────────────────┘  │
│                                        │
│   [取消]  [仅导入成功数据]  [修正后重试]│
└────────────────────────────────────────┘
```

---

## 三、可视化模块

### 3.1 矩阵图表（MatrixChart）

**核心组件**：使用 ECharts Scatter（散点图）

**可视化方案**：

**方案 A：ECharts 散点图（推荐）**

```typescript
// ECharts 配置示例
const option: EChartsOption = {
  xAxis: {
    type: 'category',
    data: ['WiFi 5', 'WiFi 6', 'WiFi 6E', 'WiFi 7'],
    axisLabel: { interval: 0, rotate: 0 }
  },
  yAxis: {
    type: 'category',
    data: ['AX1800', 'AX3000', 'AX3600', 'AX5400']
  },
  series: [
    {
      type: 'scatter',
      symbolSize: 30,
      data: [
        // [x轴索引, y轴索引, 产品信息]
        [0, 0, { product: {...}, vendor: {...} }],  // WiFi 5 + AX1800
        [1, 1, { product: {...}, vendor: {...} }],  // WiFi 6 + AX3000
        // ...
      ],
      itemStyle: {
        color: (params) => {
          // 根据厂商返回颜色
          return params.data[2].vendor.color
        }
      }
    }
  ],
  tooltip: {
    formatter: (params) => {
      // 自定义 Tooltip：显示产品详情
      const { product, vendor } = params.data[2]
      return `
        <strong>${product.name}</strong><br/>
        厂商: ${vendor.name}<br/>
        价格: ¥${product.price || 'N/A'}<br/>
        ...
      `
    }
  },
  grid: {
    left: 80, right: 40, top: 60, bottom: 60,
    containLabel: true
  }
}
```

**优点**：
- 与现有技术栈一致（ECharts 5）
- 内置缩放、拖拽、Tooltip
- 支持自定义标记（颜色、形状、图片）
- 图例交互（显示/隐藏厂商）

**单元格内多产品布局**：
- **重叠模式**：在单元格中心随机偏移 ±5px，避免完全重叠
- **堆叠模式**：需要自定义渲染（Canvas 叠加层）
- **网格模式**：需要自定义渲染

---

### 3.2 厂商筛选器（VendorFilter）

**UI 组件**：Checkbox Group（横向排列）

```
┌────────────────────────────────────────┐
│ 厂商: [√] TP-Link  [√] 华为  [ ] Cisco │
│       [√] 小米     [ ] 华硕  [ ] 网件  │
└────────────────────────────────────────┘
```

**功能**：
- 点击复选框显示/隐藏对应厂商的产品
- 带厂商颜色标识（小圆点）
- [全选] / [全不选] 快捷按钮

**实时更新**：勾选状态改变时，矩阵图表立即更新

---

### 3.3 轴选择器（AxisSelector）

**UI 组件**：两个 Select 下拉框

```
┌────────────────────────────────────────┐
│ X 轴: [WiFi 协议  ▼]  Y 轴: [速率级别 ▼] │
│ [交换 X/Y 轴 ⇄]                        │
└────────────────────────────────────────┘
```

**功能**：
- 下拉选择任意维度作为 X/Y 轴
- [交换] 按钮：快速交换 X/Y 轴
- 选择改变时，重新生成矩阵图表

---

## 四、分析模块（可选功能）

### 4.1 厂商分布图（VendorDistribution）

**图表类型**：ECharts Bar Chart（堆叠柱状图）

**可视化**：
- X 轴：矩阵的所有单元格（维度组合）
- Y 轴：产品数量
- 系列：各厂商（堆叠显示）

**示例**：
```
产品数量
   │
 8 │     ■■
   │     ■■ ▓▓
 6 │ ░░  ■■ ▓▓
   │ ░░  ■■ ▓▓ ▒▒
 4 │ ░░  ■■ ▓▓ ▒▒
   │ ░░  ■■ ▓▓ ▒▒
 2 │ ░░  ■■ ▓▓ ▒▒
   │ ░░  ■■ ▓▓ ▒▒
 0 └─────────────────
     WiFi5 WiFi6 WiFi6E
     AX1800 AX3000 AX3600

 图例: ░ TP-Link  ■ 华为  ▓ Cisco  ▒ 小米
```

**洞察**：
- 各厂商在不同维度组合的产品覆盖情况
- 哪些区域竞争激烈，哪些区域是空白市场

---

### 4.2 价格热力图（PriceHeatmap）

**图表类型**：ECharts Heatmap

**可视化**：
- X 轴：X 轴维度
- Y 轴：Y 轴维度
- 颜色深浅：单元格内产品的平均价格

**示例**：
```
       WiFi 5  WiFi 6  WiFi 6E  WiFi 7
AX5400 │ 🟦   │ 🟨    │ 🟧     │ 🟥    │  价格
       ├──────┼───────┼────────┼───────┤  ↑ 高
AX3600 │ 🟦   │ 🟨    │ 🟨     │ 🟧    │  │
       ├──────┼───────┼────────┼───────┤  │
AX3000 │ 🟩   │ 🟦    │ 🟨     │ 🟨    │  │
       ├──────┼───────┼────────┼───────┤  │
AX1800 │ 🟩   │ 🟦    │ 🟦     │ 🟨    │  ↓ 低
```

**洞察**：
- 不同维度组合的价格带分布
- 高端/中端/入门产品的划分区域

---

### 4.3 覆盖率分析（CoverageAnalysis）

**展示形式**：表格 + 百分比条形图

```
┌────────────────────────────────────────┐
│ 厂商覆盖率分析 (总单元格数: 16)        │
├────────────────────────────────────────┤
│ 厂商      覆盖单元格  覆盖率  产品数   │
│ TP-Link      14      87.5%    28      │
│   ████████████████████████████░░░░     │
│ 华为         12      75.0%    19      │
│   ████████████████████████░░░░░░░░     │
│ Cisco         8      50.0%    12      │
│   ████████████████░░░░░░░░░░░░░░░░     │
│ 小米          6      37.5%     9      │
│   ████████████░░░░░░░░░░░░░░░░░░░░     │
└────────────────────────────────────────┘
```

**指标**：
- **覆盖单元格数**：该厂商至少有一个产品的单元格数量
- **覆盖率**：覆盖单元格数 / 总单元格数
- **产品数**：该厂商的总产品数量

**洞察**：
- 各厂商的市场覆盖广度
- 哪些厂商产品线最完整

---

## 五、数据导入导出

### 5.1 Excel 导出（ExcelExporter）

**导出内容**：5 个工作表

**工作表 1: 产品列表**
| 产品名称 | 厂商 | 品牌 | 型号 | 价格 | 描述 | [维度1] | [维度2] | ... |
|----------|------|------|------|------|------|---------|---------|-----|
| ...      | ...  | ...  | ...  | ...  | ...  | ...     | ...     | ... |

**工作表 2: 厂商配置**
| 厂商名称 | 颜色 (HEX) | 显示顺序 |
|----------|-----------|----------|
| TP-Link  | #00A3E0   | 0        |
| ...      | ...       | ...      |

**工作表 3: 维度定义**
| 维度名称 | 类型 | 选项/范围 | 显示顺序 |
|----------|------|-----------|----------|
| WiFi 协议 | 离散 | WiFi 5, WiFi 6, WiFi 6E, WiFi 7 | 0 |
| 价格区间 | 连续 | 0-5000 元 | 2 |

**工作表 4: 矩阵统计**
| 统计项 | 数值 |
|--------|------|
| 总产品数 | 68 |
| 总厂商数 | 6 |
| 总维度数 | 5 |
| 平均价格 | ¥892 |
| 价格中位数 | ¥599 |
| 矩阵单元格数 | 16 (4×4) |
| 非空单元格数 | 14 |
| 覆盖率 | 87.5% |

**工作表 5: 产品矩阵数据**（透视表格式）
|          | WiFi 5 | WiFi 6 | WiFi 6E | WiFi 7 |
|----------|--------|--------|---------|--------|
| AX1800   | 5      | 8      | 3       | 1      |
| AX3000   | 4      | 12     | 6       | 2      |
| AX3600   | 2      | 9      | 7       | 3      |
| AX5400   | 0      | 3      | 5       | 4      |

*（数字表示该单元格的产品数量）*

---

### 5.2 Excel 导入（ExcelImporter）

**支持格式**：
- 仅产品列表（工作表 1）
- 完整配置（工作表 1-3）

**导入模式**：
- **追加模式**（默认）：新增产品，不修改现有数据
- **覆盖模式**：删除现有产品，完全替换
- **智能合并**：根据产品名称/型号判断，存在则更新，不存在则新增

**数据映射**：
- 厂商名称自动匹配（不存在则提示创建）
- 维度值自动验证和转换
- 价格字符串解析（支持 "¥399", "399元", "399.00"）

---

### 5.3 模板生成（TemplateGenerator）

**功能**：根据当前维度定义，动态生成 Excel 模板

**模板内容**：
- 工作表 1：产品列表（列头根据维度定义动态生成）
- 工作表 2：维度选项参考（列出所有离散维度的可选值）
- 工作表 3：填表说明

**工作表 3: 填表说明**
```
产品矩阵导入模板使用说明

1. 必填字段（标*）：产品名称、厂商、以及所有标记为必填的维度
2. 厂商名称必须在系统中存在，如需新增厂商请先在系统中添加
3. 离散维度的值必须从"维度选项参考"工作表中选择
4. 连续维度的值必须在规定范围内（参见维度定义）
5. 价格字段支持多种格式：399、¥399、399元 均可识别
6. 填写完成后，点击"批量导入"上传此文件

如有疑问，请参阅用户手册或联系技术支持。
```

---

## 六、UI 布局设计

### 主视图布局

```
┌─────────────────────────────────────────────────────────┐
│ Navbar (顶部导航栏)                    [主题] [语言] [用户]│
├─────────────────────────────────────────────────────────┤
│                                                           │
│  ┌─────────────────────────────────────────────────┐    │
│  │ 📊 Radar 1 │ 📈 Timeline │ 👥 Manpower │ [🔲 产品矩阵] │+ │    │
│  └─────────────────────────────────────────────────┘    │
│                                                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │ [Toolbar]                                         │  │
│  │ 厂商: [√]TP-Link [√]华为 [√]Cisco                │  │
│  │ X轴:[WiFi协议▼] Y轴:[速率级别▼] [⇄交换] [导出]  │  │
│  ├───────────────────────────────────────────────────┤  │
│  │                                                   │  │
│  │          产品矩阵图表 (ECharts Scatter)           │  │
│  │                                                   │  │
│  │     Y轴                                           │  │
│  │      │  ┌───┬───┬───┬───┐                       │  │
│  │  AX5400 │ ● │ ●○│   │ ○ │  图例:                │  │
│  │      │  ├───┼───┼───┼───┤  ● TP-Link            │  │
│  │  AX3600 │ ● │●●○│ ○ │   │  ○ 华为               │  │
│  │      │  ├───┼───┼───┼───┤  ■ Cisco              │  │
│  │  AX3000 │ ○ │ ●■│ ○ │ ● │                       │  │
│  │      │  ├───┼───┼───┼───┤                       │  │
│  │  AX1800 │●●○│ ○ │ ■ │ ○ │                       │  │
│  │      │  └───┴───┴───┴───┘                       │  │
│  │         WiFi5 WiFi6 WiFi6E WiFi7 (X轴)          │  │
│  │                                                   │  │
│  │   [💡 洞察] 覆盖率: TP-Link 87.5% | 华为 75.0% ...│  │
│  └───────────────────────────────────────────────────┘  │
│                                                           │
│  [⚙️ 设置]  (浮动按钮)                                   │
│                                                           │
└─────────────────────────────────────────────────────────┘
```

### 设置抽屉布局

```
┌─────────────────────────────────┐
│ 产品矩阵设置              [× 关闭] │
├─────────────────────────────────┤
│                                 │
│ ► 厂商管理 (6)                  │
│   ├─ ● TP-Link                  │
│   ├─ ● 华为                     │
│   ├─ ■ Cisco                    │
│   ├─ ○ 小米                     │
│   └─ [+ 添加厂商]               │
│                                 │
│ ► 维度定义 (5)                  │
│   ├─ WiFi 协议 (离散, 4选项)    │
│   ├─ 速率级别 (离散, 4选项)     │
│   ├─ 价格区间 (连续, 0-5000元) │
│   └─ [+ 添加维度]               │
│                                 │
│ ► 产品管理 (68)                 │
│   ├─ [产品列表]                 │
│   ├─ [+ 添加产品]               │
│   └─ [📥 批量导入]              │
│                                 │
│ ► 矩阵配置                      │
│   ├─ X 轴: [WiFi 协议  ▼]       │
│   ├─ Y 轴: [速率级别   ▼]       │
│   ├─ [√] 使用厂商颜色           │
│   ├─ [√] 显示价格标签           │
│   └─ 布局: [重叠 ▼]             │
│                                 │
│ ► 统计分析                      │
│   ├─ [📊 厂商分布图]            │
│   ├─ [🔥 价格热力图]            │
│   └─ [📈 覆盖率分析]            │
│                                 │
│ ► 数据管理                      │
│   ├─ [📤 导出 Excel]            │
│   ├─ [📄 导出 JSON]             │
│   └─ [📋 下载模板]              │
│                                 │
└─────────────────────────────────┘
```

---

## 七、交互流程

### 7.1 新建产品矩阵流程

```
1. 点击 Tab 栏的 [+] 按钮
   └─> 选择 "产品矩阵"
       └─> 创建默认配置的矩阵 Tab

2. 点击 [⚙️ 设置] 打开设置抽屉

3. 配置厂商
   └─> [+ 添加厂商] → 输入名称、选择颜色
   └─> 重复添加多个厂商 (如 TP-Link, 华为, Cisco...)

4. 定义维度
   └─> [+ 添加维度] → 选择类型、配置选项/范围
   └─> 至少添加 2 个维度 (用作 X/Y 轴)

5. 录入产品
   └─> 方式 A: [+ 添加产品] → 逐个手动录入
   └─> 方式 B: [批量导入] → 使用 Excel 批量导入

6. 配置矩阵显示
   └─> 选择 X/Y 轴维度
   └─> 设置显示选项 (颜色、布局等)

7. 查看矩阵图表
   └─> 自动生成二维矩阵
   └─> 筛选、分析、导出
```

### 7.2 矩阵交互流程

```
用户操作                    系统响应
────────────────────────────────────────────
悬停产品标记          →    显示 Tooltip (产品详情)
点击产品标记          →    打开产品详情抽屉
点击厂商复选框        →    显示/隐藏该厂商产品
改变 X/Y 轴选择       →    重新渲染矩阵图表
点击空单元格          →    提示 "此区域暂无产品"
拖拽图表             →    平移视图 (大矩阵时)
滚轮缩放             →    放大/缩小图表 (大矩阵时)
点击 [导出]          →    下载 Excel 文件
点击 [📊 统计分析]    →    打开分析模态框
```

### 7.3 产品编辑流程

```
1. 在产品列表中点击 [编辑]
   └─> 打开产品编辑弹窗，加载现有数据

2. 修改产品信息
   └─> 通用字段：名称、厂商、价格等
   └─> 维度数据：离散选择、连续输入

3. 表单验证
   └─> 实时验证：必填、范围、重复
   └─> 显示错误提示

4. 点击 [保存]
   └─> 提交数据 → Zustand Store 更新
   └─> 自动保存到 Supabase (防抖 500ms)
   └─> 关闭弹窗，刷新产品列表和矩阵图表

5. 点击 [取消]
   └─> 放弃修改，关闭弹窗
```

---

## 八、技术实现细节

### 8.1 坐标映射算法

**离散维度映射**：
```typescript
function mapDiscreteToIndex(dimensionId: string, value: string): number {
  const dimension = getDimension(dimensionId)
  const optionIndex = dimension.options.findIndex(opt => opt.id === value)
  return optionIndex
}

// 示例：WiFi 6 → 索引 1
```

**连续维度映射**（需要分段）：
```typescript
function mapContinuousToIndex(dimensionId: string, value: number): number {
  const dimension = getDimension(dimensionId)
  const { min, max, segments } = dimension.range
  const segmentSize = (max - min) / segments
  const index = Math.floor((value - min) / segmentSize)
  return Math.min(index, segments - 1)  // 限制在范围内
}

// 示例：价格 399 元，范围 0-5000，5 段
// → 分段大小 1000
// → 索引 = floor(399 / 1000) = 0
```

### 8.2 单元格内多产品布局算法

**重叠模式（推荐）**：
```typescript
function calculateProductPosition(
  cellCenterX: number,
  cellCenterY: number,
  productIndex: number,
  totalProducts: number
): [number, number] {
  if (totalProducts === 1) {
    return [cellCenterX, cellCenterY]
  }

  // 圆形分布：产品围绕单元格中心呈圆形排列
  const radius = 10  // 偏移半径 (像素)
  const angle = (productIndex / totalProducts) * 2 * Math.PI
  const offsetX = radius * Math.cos(angle)
  const offsetY = radius * Math.sin(angle)

  return [cellCenterX + offsetX, cellCenterY + offsetY]
}
```

**堆叠模式**（需要自定义渲染）：
- 使用 Canvas 在 ECharts 图表上叠加层
- 每个单元格绘制产品卡片列表
- 支持展开/折叠（超过 N 个产品时）

### 8.3 数据验证规则

```typescript
// 产品验证规则
const productValidationRules = {
  name: {
    required: true,
    message: '产品名称不能为空'
  },
  vendorId: {
    required: true,
    message: '请选择厂商'
  },
  price: {
    type: 'number',
    min: 0,
    message: '价格必须为非负数'
  },
  dimensionValues: {
    validator: (values: Record<string, DimensionValue>) => {
      // 检查必填维度
      for (const dim of getRequiredDimensions()) {
        if (!values[dim.id]) {
          return `请填写 ${dim.name}`
        }
      }

      // 检查连续维度范围
      for (const [dimId, val] of Object.entries(values)) {
        if (val.type === 'continuous') {
          const dim = getDimension(dimId)
          if (val.value < dim.range.min || val.value > dim.range.max) {
            return `${dim.name} 必须在 ${dim.range.min}-${dim.range.max} ${dim.range.unit} 范围内`
          }
        }
      }

      return null  // 验证通过
    }
  }
}
```

---

## 九、性能优化

### 9.1 大数据量优化

**场景**：产品数量 > 200 时

**优化方案**：
1. **虚拟化渲染**：产品列表使用 Ant Design Table 的 `virtual` 属性
2. **分页加载**：矩阵图表按单元格懒加载产品数据
3. **Canvas 降级**：ECharts 自动切换到 Canvas 渲染模式
4. **数据采样**：统计分析时对数据进行采样（如只分析前 500 个产品）

### 9.2 渲染优化

```typescript
// 使用 useMemo 缓存图表配置
const chartOption = useMemo(() => {
  return generateMatrixChartOption(products, dimensions, config)
}, [products, dimensions, config])

// 防抖保存
const debouncedSaveChart = useDebouncedCallback((chart: ProductMatrixChart) => {
  supabaseService.radarCharts.update(chart)
}, 500)
```

### 9.3 交互响应优化

- **Tooltip 延迟**：鼠标悬停 200ms 后再显示 Tooltip
- **筛选防抖**：厂商筛选改变后 300ms 才更新图表
- **滚动节流**：大矩阵滚动时，每 100ms 更新一次视图

---

## 十、国际化支持

### 中文语言包示例

```typescript
export const zhCN_ProductMatrix = {
  productMatrix: {
    title: '产品矩阵',
    createNew: '新建产品矩阵',

    // 厂商管理
    vendor: {
      title: '厂商管理',
      add: '添加厂商',
      edit: '编辑厂商',
      delete: '删除厂商',
      name: '厂商名称',
      color: '品牌颜色',
      confirmDelete: '确定删除厂商"{name}"？',
      hasProducts: '该厂商下有 {count} 个产品，删除后产品将失去厂商关联。'
    },

    // 维度定义
    dimension: {
      title: '维度定义',
      add: '添加维度',
      edit: '编辑维度',
      delete: '删除维度',
      name: '维度名称',
      type: '维度类型',
      discrete: '离散型',
      continuous: '连续型',
      options: '选项列表',
      addOption: '添加选项',
      range: '数值范围',
      min: '最小值',
      max: '最大值',
      unit: '单位',
      segments: '建议分段'
    },

    // 产品管理
    product: {
      title: '产品管理',
      add: '添加产品',
      edit: '编辑产品',
      delete: '删除产品',
      import: '批量导入',
      export: '导出选中',
      downloadTemplate: '下载模板',
      name: '产品名称',
      vendor: '所属厂商',
      brand: '品牌',
      model: '型号',
      price: '价格',
      description: '描述',
      imageUrl: '产品图片',
      confirmDelete: '确定删除产品"{name}"？',
      deleteSuccess: '产品删除成功'
    },

    // 矩阵配置
    matrix: {
      title: '矩阵配置',
      xAxis: 'X 轴维度',
      yAxis: 'Y 轴维度',
      swapAxes: '交换 X/Y 轴',
      showVendorColors: '使用厂商颜色',
      showProductImages: '显示产品图片',
      showPriceLabels: '显示价格标签',
      showEmptyCells: '显示空单元格',
      cellLayout: '单元格布局',
      layoutStack: '堆叠',
      layoutGrid: '网格',
      layoutOverlap: '重叠',
      maxProductsPerCell: '单格最多显示'
    },

    // 统计分析
    analysis: {
      title: '统计分析',
      vendorDistribution: '厂商分布图',
      priceHeatmap: '价格热力图',
      coverageAnalysis: '覆盖率分析',
      totalProducts: '总产品数',
      totalVendors: '总厂商数',
      coverageRate: '覆盖率'
    }
  }
}
```

---

## 十一、错误处理

### 常见错误场景

| 错误场景 | 错误提示 | 处理方式 |
|----------|----------|----------|
| 未配置维度 | "请先定义至少 2 个维度" | 引导用户到维度定义 |
| 维度选项不足 | "离散维度至少需要 2 个选项" | 禁用保存按钮 |
| X/Y 轴相同 | "X 轴和 Y 轴不能选择相同维度" | 自动调整或提示 |
| 产品缺少维度数据 | "产品 {name} 缺少维度 {dim}" | 标记为警告，矩阵中不显示 |
| Excel 导入失败 | "第 {row} 行：{error}" | 显示详细错误列表 |
| 网络请求失败 | "保存失败，请检查网络连接" | 重试按钮 |

---

## 十二、可访问性（Accessibility）

- 键盘导航：支持 Tab、Enter、Esc 快捷键
- ARIA 标签：为图表元素添加语义化标签
- 颜色盲友好：厂商颜色支持形状辅助（如 ●○■△）
- 高对比度：支持高对比度主题（深色模式优化）
